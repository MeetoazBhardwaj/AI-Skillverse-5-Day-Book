# Diagrams

This folder contains Mermaid diagrams used across the AI Skillverse book.

To use a diagram inside a markdown file, copy the content of a `.mmd` file into a Mermaid code block:

```text
```mermaid
flowchart TD
    A --> B
```
```

GitHub can render Mermaid diagrams directly in markdown files.
